# Rewrite roadmap

Current target: **M1 - Session core**.

The next implementation task is to create two minimal native executables, Host and Viewer,
plus a shared core library. They must connect over localhost, exchange a versioned packet,
and reconnect cleanly. Screen capture and UI come after this works reliably.
